name: CI — ProctoAI MERN

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:

  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:6
        ports:
          - 27017:27017

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Backend Dependencies
        run: npm install

      - name: Run ESLint
        run: npm run lint || true

      - name: Run Backend Tests
        env:
          MONGO_URI: mongodb://localhost:27017/proctoai_test
          JWT_SECRET: test-secret
        run: npm test || true

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Frontend Dependencies
        run: npm install

      - name: Run ESLint
        run: npm run lint || true

      - name: Build React App
        run: npm run build
